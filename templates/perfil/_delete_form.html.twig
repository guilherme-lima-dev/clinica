{#<div class="btn">#}
    <form method="post" action="{{ path('perfil_delete', {'idperfil': perfil.idperfil}) }}"
          onsubmit="return confirm('Você tem certeza que deseja excluir esse item?');">
        <input type="hidden" name="_method" value="DELETE">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ perfil.idperfil) }}">
        <button class="mb-2 mr-2 btn btn-danger">Deletar</button>
    </form>
{#</div>#}