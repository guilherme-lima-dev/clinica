{{ form_start(form) }}
    {{ form_widget(form) }}
<hr>
<a class="btn btn-secondary" href="{{ path('prontuarios_index') }}">Voltar à Lista</a>
<button class="btn btn-success">{{ button_label|default('Salvar') }}</button>
{{ form_end(form) }}
{% if app.request.pathinfo != '/prontuarios/new' %}
    {{ include('prontuarios/_delete_form.html.twig') }}
{% endif %}
