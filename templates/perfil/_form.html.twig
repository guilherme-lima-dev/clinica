{{ form_start(form) }}
    {{ form_widget(form) }}
<hr>
<a class="mb-2 mr-2 btn btn-secondary" href="{{ path('perfil_index') }}">Voltar à Lista</a>
<button class="mb-2 mr-2 btn btn-success">{{ button_label|default('Salvar') }}</button>
{{ form_end(form) }}
{% if app.request.pathinfo != '/perfil/new' %}
    {{ include('perfil/_delete_form.html.twig') }}
{% endif %}
