<form method="post" action="{{ path('acesso_delete', {'id': acesso.id}) }}" onsubmit="return confirm('Você tem certeza que deseja excluir esse item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ acesso.id) }}">
    <button class="btn btn-danger">Deletar</button>
</form>
